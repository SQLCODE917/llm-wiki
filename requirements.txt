# Core dependencies
pyyaml>=6.0
python-frontmatter>=1.0  # YAML frontmatter parsing
ftfy>=6.0                # Unicode text normalization
cattrs>=23.0             # Dataclass serialization
json-repair>=0.25        # LLM JSON repair

# Local packages (install in editable mode)
-e packages/wiki_core
-e packages/wiki_io

# Optional: Cloud model backends (install as needed)
# pip install boto3      # For AWS Bedrock
# pip install openai     # For OpenAI API
# pip install anthropic  # For Anthropic API

# Development
pytest>=7.0
